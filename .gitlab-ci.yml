stages:
  - deploy
deploy:
  stage: deploy
  image: devth/helm
  script:
    - helm init --upgrade
    - helm upgrade
      --install dmitry-guest-book-php 
      --namespace development 
      --wait 
      --set replicaCount=3
      ./helm-data/